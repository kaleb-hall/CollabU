# CollabU - Student Deadline Management Platform

## Setup
1. Backend: `cd backend && python -m venv venv && source venv/bin/activate && pip install -r requirements.txt`
2. Frontend: `cd frontend && npm install && npm run dev`
